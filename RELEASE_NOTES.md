Исправлено сжатие видео.

- Ошибка «FFMPEG код 3752568763» на части файлов. Вписывание кадра в
  выбранное разрешение давало нечётную сторону, а кодировщик x264 такие
  не принимает. Теперь размер всегда приводится к чётному
- Файл, ставший больше исходного, больше не показывается как успешное
  сжатие с процентом вида «--56%». Программа честно пишет «файл вырос»
  и советует увеличить CRF
- При сбое в журнал попадает сообщение самого FFMPEG, а не только
  числовой код — по нему видно, что именно не понравилось
