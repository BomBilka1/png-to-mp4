Технический релиз — первая сборка через GitHub Actions.

- Установщик собирается на Windows-раннере GitHub, а не вручную
- Размер установщика уменьшился со 107 до 91 МБ: из сборки убраны
  дублирующиеся копии moviepy, imageio, imageio_ffmpeg и piexif

Изменений в самой программе нет.
