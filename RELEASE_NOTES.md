Исправлено сжатие PDF.

- Сжатие падало с ошибкой «Page must be part of a PdfWriter» и не
  обрабатывало ни одного файла
- Сжатие переведено на PyMuPDF: чистятся неиспользуемые объекты,
  пережимаются шрифты и картинки — результат заметно меньше прежнего
